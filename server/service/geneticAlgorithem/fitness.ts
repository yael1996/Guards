export class Fitness {
  constructor() {}

  public doAction(phenotype) {
    var fitness = 0;
    // use phenotype and possibly some other information
    // to determine the fitness number.  Higher is better, lower is worse.
    return fitness;
  }
}
